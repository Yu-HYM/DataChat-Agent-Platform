# DataChat 启停操作手册

> 适用于 Docker Compose 部署模式（推荐）
> 环境：Windows 11 + Docker Desktop（WSL2后端）+ Ollama（宿主机 GPU）+ WSL2 Ubuntu-22.04（MySQL）

---

## 一、启动流程

按顺序在 PowerShell 中执行（**顺序不能乱**）：

### Step 1：启动 Ollama（模型推理服务）

```powershell
# 如果 Ollama 没启动（重启电脑后需要手动启）
E:\简历项目\03-智能问数Agent平台\Ollama\ollama.exe serve
```

**注意**：这条命令是前台运行，**不要关这个窗口**，关掉 Ollama 就停了。可以最小化放后台。

验证 Ollama 正常：新开一个 PowerShell 执行
```powershell
E:\简历项目\03-智能问数Agent平台\Ollama\ollama.exe list
```
能看到 `qwen2.5:7b` 和 `bge-m3` 即正常。

---

### Step 2：启动 WSL（MySQL 会自动拉起）

```powershell
wsl -d Ubuntu-22.04 -e "mysqladmin -uroot -p123456 ping"
```

预期输出：`mysqld is alive`（如果没启动会报 10061，等 30 秒再执行一次）。

**解释**：WSL 已配置 wsl.conf，MySQL 开机自启，所以只要 WSL 启动 MySQL 就自动拉起来。

---

### Step 3：启动 DataChat（Docker 容器）

```powershell
cd E:\简历项目\03-智能问数Agent平台\datachat
docker compose up -d
```

首次启动或代码有改动时需要加 `--build`：
```powershell
docker compose up -d --build
```

**预期输出**：
```
[+] Running 3/3
 ✔ Container datachat-api     Started   (healthy)
 ✔ Container datachat-web     Started
```

等待约 30 秒（API 容器需要健康检查通过），然后：

---

### Step 4：验证

| 服务 | 访问地址 | 预期 |
|---|---|---|
| Streamlit 前端 | http://localhost:8501 | 打开 DataChat 主界面，可正常问答 |
| FastAPI API | http://localhost:9000 | 返回 `{"service":"DataChat","status":"ok"}` |
| Ollama | http://localhost:11434 | 返回 "Ollama is running" |

**快速验证命令**：
```powershell
# API 健康检查
curl http://localhost:9000/

# 测试 Agent 问答
$token = (curl -s "http://localhost:9000/auth/token" | ConvertFrom-Json).token
curl -s -X POST "http://localhost:9000/chat?q=最近三天GMV是多少&thread_id=test1" `
  -H "Authorization: Bearer $token"
```

---

## 二、停止流程

### 快速全部关闭

```powershell
# 1. 停 Docker 容器
cd E:\简历项目\03-智能问数Agent平台\datachat
docker compose down

# 2. 停 WSL（MySQL 随之停止）
wsl --shutdown

# 3. 关 Ollama（可选，想留着就不用关）
taskkill /F /IM ollama.exe
```

关闭顺序无强制要求，以上三条独立执行即可。

---

## 三、常用运维命令

### 3.1 容器状态

```powershell
cd E:\简历项目\03-智能问数Agent平台\datachat

# 查看状态
docker compose ps

# 查看 API 日志
docker compose logs -f api

# 查看前端日志
docker compose logs -f web

# 进入 API 容器调试
docker compose exec api bash
```

### 3.2 重启单个服务

```powershell
# 只重启 API
docker compose restart api

# 只重启前端
docker compose restart web
```

### 3.3 代码修改后更新

```powershell
# 重新构建镜像并启动
cd E:\简历项目\03-智能问数Agent平台\datachat
docker compose up -d --build
```

### 3.4 重建 RAG 知识库

```powershell
# 方式1：通过 API 上传单个文件
curl -X POST "http://localhost:9000/kb/upload" `
  -H "Authorization: Bearer <你的token>" `
  -F "file=@kb_docs\A01-指标口径手册.md"

# 方式2：进容器命令行全量重建
docker compose exec api python -m app.rag.ingest
```

---

## 四、常见问题排查

### Q1: `docker compose up` 报端口 9000/8501 被占用？

```powershell
# 查占用进程
netstat -ano | findstr :9000
netstat -ano | findstr :8501

# 杀掉占用进程（用上面查到的 PID 替换 <PID>）
taskkill /PID <PID> /F
```

### Q2: API 容器报健康检查失败？

```powershell
# 看日志
docker compose logs api

# 常见原因：
# 1. Ollama 没启动 → 回 Step 1 启 Ollama
# 2. WSL/MySQL 没启动 → 回 Step 2 启 WSL
# 3. host.docker.internal 不通 → 检查 Docker Desktop 是否在运行
```

### Q3: Agent 回答很慢或报错"连接超时"？

检查 Ollama 是否正常：
```powershell
curl http://localhost:11434/v1/chat/completions `
  -H "Content-Type: application/json" `
  -d '{"model":"qwen2.5:7b-instruct-q4_K_M","messages":[{"role":"user","content":"你好"}]}'
```

能回复说明 Ollama 正常，问题出在容器网络。

### Q4: 模型卸载后首次调用慢？

正常现象。设置 `OLLAMA_KEEP_ALIVE=5m` 控制卸载时机（不设置默认就是 5 分钟），可以调长一些。

### Q5: 想清空所有数据重新开始？

```powershell
# 只清容器和镜像，保留代码
docker compose down -v --rmi local

# 清 ChromaDB 向量库（慎用，重建需重新向量化）
Remove-Item -Recurse -Force "E:\简历项目\03-智能问数Agent平台\datachat\chroma_db"
```

---

## 五、两种运行模式对比

| 维度 | Docker Compose（推荐） | 本地 venv 开发模式 |
|---|---|---|
| 启动命令 | `docker compose up -d` | 两个终端分别起 uvicorn 和 streamlit |
| 环境一致性 | ✅ 在哪台电脑都一样 | ❌ Python 版本/依赖容易冲突 |
| 资源占用 | 略高（容器开销） | 略低 |
| 端口 | 固定 9000/8501 | 同上 |
| 适用场景 | 演示/交付 | 调试代码/改逻辑 |
